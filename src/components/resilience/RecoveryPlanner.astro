---
import { RECOVERY_QUESTIONS } from '../../data/resilience-planner';
import ChooserQuestion from '../platform/ChooserQuestion.astro';
import GuideNextSteps from '../shared/GuideNextSteps.astro';
---
<section class="recovery-planner" id="recovery-planner" aria-labelledby="recovery-planner-title">
  <h2 id="recovery-planner-title">Prepare a recovery brief.</h2>
  <p class="recovery-planner__lede">Five questions about one business operation. Get priorities, unresolved constraints and what to test next. These are planning prompts, not guaranteed recovery times or a compliance decision.</p>
  <noscript><p>Enable JavaScript to prepare the brief. The four explanations above and the reading below remain available without it.</p></noscript>
  <div class="recovery-planner__grid">
    <form data-recovery-form>
      <ol role="list">{RECOVERY_QUESTIONS.map((question,index)=><ChooserQuestion question={{...question,options:[...question.options]}} index={index} total={RECOVERY_QUESTIONS.length} />)}</ol>
      <button type="button" data-recovery-reset disabled>↺ Start over</button>
    </form>
    <aside class="recovery-planner__result">
      <div class="recovery-planner__toolbar">
        <p data-recovery-progress aria-live="polite" aria-atomic="true">0 of 5 answered</p>
        <button type="button" data-recovery-reset disabled>↺ Start over</button>
      </div>
      <div class="recovery-planner__body" tabindex="0" role="region" aria-label="Recovery priorities">
        <p data-recovery-summary>Complete the five questions to prepare a recovery brief.</p>
        <div data-recovery-output hidden>
          <h3>Recovery priorities</h3>
          <div data-recovery-gaps hidden><h4>Resolve first</h4><ul></ul></div>
          <ol data-recovery-priorities></ol>
        </div>
        <GuideNextSteps current="resilience" />
      </div>
    </aside>
  </div>
</section>
<script>
  import { initializeRecoveryPlanner } from '../../scripts/resilience-planner';
  initializeRecoveryPlanner();
  document.addEventListener('astro:page-load', initializeRecoveryPlanner);
</script>
<style>
  h2 { font: var(--weight-semibold) var(--text-headline-md)/var(--leading-snug) var(--font-display); letter-spacing: var(--tracking-tight); }
  .recovery-planner__lede { max-width: 75ch; margin-block: var(--space-3) var(--space-6); color: var(--color-on-surface-muted); line-height: var(--leading-relaxed); }
  .recovery-planner__grid { display: grid; gap: var(--space-6); align-items: start; }
  form > ol { display: grid; gap: var(--space-5); }
  button { border: 0; padding: var(--space-3); background: var(--color-primary-tint); color: var(--color-navy); font: var(--text-label-md) var(--font-body); cursor: pointer; }
  form > button { margin-top: var(--space-4); }
  button:disabled { opacity: .5; cursor: default; }
  button:hover:enabled { background: var(--color-navy); color: var(--color-on-primary); }
  .recovery-planner__result { display: flex; flex-direction: column; min-width: 0; background: var(--color-surface-container-low); }
  .recovery-planner__toolbar { display: flex; gap: var(--space-3); align-items: center; justify-content: space-between; padding: var(--space-4); }
  .recovery-planner__toolbar p { font: var(--text-label-sm) var(--font-mono); color: var(--color-primary); }
  .recovery-planner__body { padding: var(--space-5); min-height: 0; line-height: var(--leading-relaxed); overflow-wrap: anywhere; }
  [hidden] { display: none; }
  h3 { margin-top: var(--space-4); font-size: var(--text-title-lg); color: var(--color-navy); }
  [data-recovery-gaps] { padding: var(--space-4); margin-block: var(--space-4); background: var(--color-primary-tint); }
  [data-recovery-gaps] ul { list-style: disc; padding-left: var(--space-4); }
  .recovery-planner__body :global(li) { margin-top: var(--space-3); }
  .recovery-planner__body :global(h4) { color: var(--color-navy); font-weight: var(--weight-semibold); }
  .recovery-planner__body :global(li p) { font-size: var(--text-label-md); color: var(--color-on-surface-muted); }
  @media(min-width: 1024px) {
    .recovery-planner__grid { grid-template-columns: minmax(0,1fr) minmax(0,24rem); gap: var(--space-8); }
    .recovery-planner__result { position: sticky; top: calc(var(--nav-height) + var(--space-4)); max-height: calc(100dvh - var(--nav-height) - var(--space-8)); }
    .recovery-planner__body { overflow-y: auto; scrollbar-gutter: stable; }
  }
</style>
